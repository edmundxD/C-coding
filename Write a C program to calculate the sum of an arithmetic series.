#include <stdio.h>

int arithmeticSum(int a, int d, int n) {
    return n * (2 * a + (n - 1) * d) / 2;
}

int main() {
    int a = 2, d = 3, n = 10;
    printf("Sum: %d", arithmeticSum(a, d, n));
    return 0;
}
